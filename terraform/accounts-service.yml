#file: noinspection MaybeTerraformTemplateInspection
openapi: 3.0.1
info:
  version: "3.0"
  title: Compute Resources API
  description: |
    This is the serverless Compute Resources API
  termsOfService: https://docs.pennsieve.io/page/pennsieve-terms-of-use
  contact:
    name: Pennsieve Support
    email: support@pennsieve.net
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
servers:
  - url: https://api2.pennsieve.io/compute/resources
    description: Production server
  - url: https://api2.pennsieve.net/compute/resources
    description: Development server
externalDocs:
  description: Find more info here
  url: https://docs.pennsieve.io
tags:
  - name: Accounts Service
    description: Management of external accounts that can provide compute and/or storage capabilities.
    externalDocs:
      url: https://docs.pennsieve.io/reference
  - name: Compute Nodes
    description: Management of compute nodes for running analytical workflows on the Pennsieve platform.
components:
  x-amazon-apigateway-integrations:
    account-service:
      type: aws_proxy
      uri: ${account_service_lambda_arn}
      httpMethod: POST
      passthroughBehavior: when_no_match
      contentHandling: CONVERT_TO_TEXT
      payloadFormatVersion: 2.0
  securitySchemes:
    token_dataset_auth:
      type: "apiKey"
      name: "Unused"
      in: "header"
      x-amazon-apigateway-authorizer:
        identitySource: "$request.header.Authorization,$request.querystring.dataset_id"
        authorizerUri: ${authorize_lambda_invoke_uri}
        authorizerPayloadFormatVersion: "2.0"
        authorizerResultTtlInSeconds: 300
        type: "request"
        enableSimpleResponses: true
        authorizerCredentials: ${gateway_authorizer_role}
    token_auth:
      type: "apiKey"
      name: "Authorization"
      in: "header"
      x-amazon-apigateway-authorizer:
        identitySource: "$request.header.Authorization"
        authorizerUri: ${authorize_lambda_invoke_uri}
        authorizerPayloadFormatVersion: "2.0"
        authorizerResultTtlInSeconds: 300
        type: "request"
        enableSimpleResponses: true
        authorizerCredentials: ${gateway_authorizer_role}
    token_workspace_auth:
      type: "apiKey"
      name: "Authorization"
      in: "header"
      x-amazon-apigateway-authorizer:
        identitySource: "$request.header.Authorization,$request.querystring.organization_id"
        authorizerUri: ${authorize_lambda_invoke_uri}
        authorizerPayloadFormatVersion: "2.0"
        authorizerResultTtlInSeconds: 300
        type: "request"
        enableSimpleResponses: true
        authorizerCredentials: ${gateway_authorizer_role}
  schemas:
    accountRequest:
      type: object
      properties:
        accountId:
          type: string
          description: the registered accountId
        accountType:
          type: string
          description: the type of account
        roleName:
          type: string
          description: the name of the created role
        externalId:
          type: string
          description: a unique identifier for the role
        name:
          type: string
          description: optional name for the account
        description:
          type: string
          description: optional description for the account
    postAccountsResponse:
      type: object
      properties:
        uuid:
          type: string
          description: the uuid of the created compute resource account
    accountsGetResponse:
      type: object
      properties:
        uuid:
          type: string
          description: the uuid of the compute resource account
        accountId:
          type: string
          description: the accountId of the compute resource account
        accountType:
          type: string
          description: the account type of compute resource account
        roleName:
          type: string
          description: the name of the role in the compute resource account
        externalId:
          type: string
          description: the generated externalId for compute resource account
        organizationId:
          type: string
          description: the organization of the role creator for the compute resource account
        userId:
          type: string
          description: the user that created the role for the compute resource account
        name:
          type: string
          description: the name of the account
        description:
          type: string
          description: the description of the account
        status:
          type: string
          enum: ["Enabled", "Paused"]
          description: the status of the account
    workspaceEnablementRequest:
      type: object
      properties:
        isPublic:
          type: boolean
          description: If true, workspace administrators can manage resources on this account (subject to enableCompute/enableStorage). If false, only the account owner can manage resources.
        enableCompute:
          type: boolean
          description: If true and isPublic is true, workspace administrators can create compute nodes on this account.
        enableStorage:
          type: boolean
          description: If true and isPublic is true, workspace administrators can create storage nodes on this account.
    workspaceEnablementResponse:
      type: object
      properties:
        uuid:
          type: string
          description: the uuid of the compute resource account
        workspaceId:
          type: string
          description: the workspace ID
        isPublic:
          type: boolean
          description: If true, workspace administrators can manage resources on this account (subject to enableCompute/enableStorage). If false, only the account owner can manage resources.
        enableCompute:
          type: boolean
          description: If true and isPublic is true, workspace administrators can create compute nodes on this account.
        enableStorage:
          type: boolean
          description: If true and isPublic is true, workspace administrators can create storage nodes on this account.
        createdAt:
          type: string
          format: date-time
          description: timestamp when the enablement was created
    computeNodeRequest:
      type: object
      required:
        - name
        - accountId
      properties:
        name:
          type: string
          description: Name of the compute node
        description:
          type: string
          description: Description of the compute node
        accountId:
          type: string
          description: UUID of the compute resource account to use
        provisionerImage:
          type: string
          description: Docker image for the provisioner
          default: pennsieve/compute-node-aws-provisioner
        provisionerImageTag:
          type: string
          description: Docker tag for the provisioner image
          default: latest
        organizationId:
          type: string
          description: Organization ID (workspace) this node belongs to. Empty for organization-independent nodes.
          example: "N:organization:050fae39-4412-43ef-a514-703ed8e299d5"
        deploymentMode:
          type: string
          enum: ["basic", "secure", "compliant"]
          description: Deployment mode controlling the infrastructure networking tier
          default: basic
        enableLLMAccess:
          type: boolean
          description: Enable LLM/Bedrock access for processors via the cost governor Lambda
          default: false
        llmBaaAcknowledged:
          type: boolean
          description: >
            Confirms that an AWS Business Associate Agreement (BAA) is in place for the target account.
            Required when enableLLMAccess is true and deploymentMode is "secure" or "compliant".
          default: false
        maxGpuInstances:
          type: integer
          description: Maximum number of GPU EC2 instances in the Auto Scaling Group. Set to 0 to disable GPU support.
          default: 2
          minimum: 0
        gpuTier:
          type: string
          description: "GPU instance tier: small (T4 16GB), medium (L4 24GB), large (A10G 24GB), xlarge (L40S 48GB)"
          default: small
          enum: [small, medium, large, xlarge]
    computeNodeResponse:
      type: object
      properties:
        uuid:
          type: string
          description: UUID of the compute node
        name:
          type: string
          description: Name of the compute node
        description:
          type: string
          description: Description of the compute node
        queueUrl:
          type: string
          description: SQS Queue URL for the compute node
        account:
          type: object
          properties:
            uuid:
              type: string
              description: UUID of the account
            accountId:
              type: string
              description: AWS Account ID
            accountType:
              type: string
              description: Type of account (e.g., aws)
            ownerId:
              type: string
              description: User ID of the AWS/GCP account owner
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        organizationId:
          type: string
          description: Organization ID (workspace) this node belongs to. Empty for organization-independent nodes.
        ownerId:
          type: string
          description: User ID who created/owns this compute node
        identifier:
          type: string
          description: Unique identifier for the compute node
        workflowManagerTag:
          type: string
          description: Docker tag for the workflow manager
        provisionerImage:
          type: string
          description: Docker image for the provisioner
        provisionerImageTag:
          type: string
          description: Docker tag for the provisioner image
        deploymentMode:
          type: string
          enum: ["basic", "secure", "compliant"]
          description: Deployment mode controlling the infrastructure networking tier
        enableLLMAccess:
          type: boolean
          description: Whether LLM/Bedrock access is enabled for processors on this node
        llmBaaAcknowledged:
          type: boolean
          description: Whether a BAA acknowledgement was provided when the node was created
        maxGpuInstances:
          type: integer
          description: Maximum number of GPU EC2 instances (0 = GPU disabled)
        gpuTier:
          type: string
          description: "GPU instance tier: small (T4 16GB), medium (L4 24GB), large (A10G 24GB), xlarge (L40S 48GB)"
          enum: [small, medium, large, xlarge]
        status:
          type: string
          enum: ["Enabled", "Paused", "Pending", "Destroying"]
          description: Current status of the compute node
    computeNodeUpdateRequest:
      type: object
      properties:
        workflowManagerTag:
          type: string
          description: Docker tag for the workflow manager (optional - only for legacy provisioner)
        workflowManagerCpu:
          type: integer
          description: CPU units for the workflow manager (optional - only for legacy provisioner)
        workflowManagerMemory:
          type: integer
          description: Memory (MB) for the workflow manager (optional - only for legacy provisioner)
        authorizationType:
          type: string
          enum: ["NONE", "AWS_IAM"]
          description: Authorization type for the API Gateway (optional - only for legacy provisioner)
        provisionerImage:
          type: string
          description: Docker image for the provisioner (optional - must be whitelisted)
        provisionerImageTag:
          type: string
          description: Docker tag for the provisioner image (optional - defaults to 'latest')
        deploymentMode:
          type: string
          enum: ["basic", "secure", "compliant"]
          description: Deployment mode controlling the infrastructure networking tier (optional)
    updateConfigRequest:
      type: object
      description: Infrastructure configuration update. At least one field must be provided. Triggers re-provisioning.
      properties:
        maxGpuInstances:
          type: integer
          description: Maximum number of GPU EC2 instances. Set to 0 to disable GPU support.
          minimum: 0
        gpuTier:
          type: string
          description: "GPU instance tier: small (T4 16GB), medium (L4 24GB), large (A10G 24GB), xlarge (L40S 48GB)"
          enum: [small, medium, large, xlarge]
        enableLLMAccess:
          type: boolean
          description: Enable or disable LLM/Bedrock access for processors
    computeNodePermissionsRequest:
      type: object
      required:
        - accessScope
      properties:
        accessScope:
          type: string
          enum: ["private", "workspace", "shared"]
          description: |
            Access scope level for the compute node:
            - private: Only the owner can access
            - workspace: All workspace members can access
            - shared: Specific users and/or teams can access
        sharedWithUsers:
          type: array
          items:
            type: string
          description: List of user IDs to share with (only used when accessScope is "shared")
        sharedWithTeams:
          type: array
          items:
            type: string
          description: List of team IDs to share with (only used when accessScope is "shared")
    computeNodePermissionsResponse:
      type: object
      properties:
        nodeUuid:
          type: string
          description: UUID of the compute node
        accessScope:
          type: string
          enum: ["private", "workspace", "shared"]
          description: Current access scope level
        owner:
          type: string
          description: User ID of the node owner
        sharedWithUsers:
          type: array
          items:
            type: string
          description: List of user IDs the node is shared with
        sharedWithTeams:
          type: array
          items:
            type: string
          description: List of team IDs the node is shared with
        organizationId:
          type: string
          description: Organization ID for workspace access scope
    computeNodeAccessScopeRequest:
      type: object
      required:
        - accessScope
      properties:
        accessScope:
          type: string
          enum: ["private", "workspace", "shared"]
          description: |
            Access scope level for the compute node:
            - private: Only the owner can access
            - workspace: All workspace members can access
            - shared: Specific users and/or teams can access (existing shared access remains)
    grantUserAccessRequest:
      type: object
      required:
        - userId
      properties:
        userId:
          type: string
          description: ID of the user to grant access to
    grantTeamAccessRequest:
      type: object
      required:
        - teamId
      properties:
        teamId:
          type: string
          description: ID of the team to grant access to
    permissionActionResponse:
      type: object
      properties:
        message:
          type: string
          description: Success message
        nodeUuid:
          type: string
          description: UUID of the compute node
        action:
          type: string
          description: Action performed (granted/revoked)
        entityType:
          type: string
          enum: ["user", "team"]
          description: Type of entity the action was performed on
        entityId:
          type: string
          description: ID of the entity the action was performed on
    secretsRequest:
      type: object
      required:
        - secrets
      properties:
        secrets:
          type: object
          additionalProperties:
            type: string
          description: |
            Key-value map of secrets to store. Keys must match [A-Za-z_][A-Za-z0-9_]*.
            Maximum 50 secrets, key max 256 chars, value max 10000 chars.
            System-reserved key names (e.g. SESSION_TOKEN, DATASET_ID) are rejected.
    nodeConfigRequest:
      type: object
      properties:
        stuckTaskThresholdHours:
          type: integer
          description: |
            Number of hours after which a running ECS task is flagged as
            potentially stuck in health checks. Default: 8.
          example: 8
    nodeConfigResponse:
      type: object
      properties:
        stuckTaskThresholdHours:
          type: integer
          description: |
            Number of hours after which a running ECS task is flagged as
            potentially stuck in health checks.
          example: 8
    allowedProcessorsRequest:
      type: object
      required:
        - processors
      properties:
        processors:
          type: array
          items:
            type: string
          description: |
            List of allowed processor source URL patterns. Each entry is a source
            URL (e.g. "git://github.com/org/repo:v1"). Exact match, or omit the
            tag to allow any version (e.g. "git://github.com/org/repo").
            An empty array means all processors are allowed (open mode).
            Data targets and service processors are always allowed regardless of this list.
  responses:
    Unauthorized:
      description: Incorrect authentication or user has incorrect permissions.
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
    BadRequest:
      description: Bad Request
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
    NotFound:
      description: Not Found
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
    Error:
      description: Server Error
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
paths:
  /gpu-tiers:
    get:
      summary: List available GPU instance tiers
      description: Returns the available GPU instance tiers with specs and pricing. Use this to populate tier selection UI when creating or updating compute nodes.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getGPUTiers
      security:
        - token_workspace_auth: [ ]
      tags:
        - Compute Nodes
      responses:
        '200':
          description: List of GPU tiers
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    tier:
                      type: string
                      description: Tier label used in API requests
                    instanceType:
                      type: string
                      description: AWS EC2 instance type
                    gpu:
                      type: string
                      description: GPU model name
                    gpuMemoryGB:
                      type: integer
                      description: GPU VRAM in GB
                    vcpus:
                      type: integer
                      description: Number of vCPUs
                    memoryGB:
                      type: integer
                      description: Instance RAM in GB
                    pricePerHour:
                      type: number
                      description: On-demand price per hour (USD, us-east-1)
  /accounts/{uuid}/workspaces:
    post:
      summary: Enable a workspace for an account
      description: |
        Enables a workspace for a specific compute resource account. The isPublic flag determines
        who can create compute nodes on this account: if true, workspace managers can create nodes;
        if false, only the account owner can create nodes
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: postAccountWorkspaceEnablement
      security:
        - token_workspace_auth: [ ]
      tags:
        - Account
      parameters:
        - in: path
          name: uuid
          required: true
          schema:
            type: string
          description: The uuid of the compute resource account
        - in: query
          name: organization_id
          schema:
            type: string
          required: true
          description: Node Id of the workspace that we want to enable.
      requestBody:
        description: Workspace enablement configuration
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/workspaceEnablementRequest'
      responses:
        '201':
          description: Successfully enabled workspace for account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/workspaceEnablementResponse'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /accounts/{uuid}/workspaces/{workspaceId}:
    delete:
      summary: Disable a workspace for an account
      description: |
        Disables a workspace for a specific compute resource account
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: deleteAccountWorkspaceEnablement
      security:
        - token_auth: [ ]
      tags:
        - Account
      parameters:
        - in: path
          name: uuid
          required: true
          schema:
            type: string
          description: The uuid of the compute resource account
        - in: path
          name: workspaceId
          required: true
          schema:
            type: string
          description: The workspace ID to disable
      responses:
        '204':
          description: Successfully disabled workspace for account
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /accounts:
    post:
      summary: Registers an external compute resource account
      description: |
        Registers an external compute resource (such as AWS). This method assumes that this account has a role that
        the Pennsieve platform can assume to deploy infrastructure for the compute nodes.
        The Pennsieve Agent handles the creation of this role.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: postAccounts
      security:
        - token_auth: [ ]
      tags:
        - Account
      requestBody:
        description: Payload
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/accountRequest'
      responses:
        '201':
          description: Successfully created compute resource account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postAccountsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          description: Unprocessable Entity - Account already exists for this user
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "PostAccountsHandler: error records exists"
        '5XX':
          $ref: '#/components/responses/Error'
    get:
      summary: Get account details for the authenticated user
      description: |
        Get account details for compute resources belonging to the authenticated user.
        Optionally filter by workspace or include workspace enablement details.

        When filtering by workspace, workspace administrators (permission_bit >= 16) will also
        see accounts with isPublic=true that they don't own, enabling them to create and manage
        compute nodes on those accounts.
      parameters:
        - in: query
          name: organization_id
          schema:
            type: string
          required: false
          description: Filter accounts by organization ID (only returns accounts enabled for this organization). Admins will also see public accounts (isPublic=true) they don't own.
        - in: query
          name: includeWorkspaces
          schema:
            type: string
            enum: ["true", "false"]
          required: false
          description: Include workspace enablement details in the response
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getAccounts
      security:
        - token_auth: [ ]
      tags:
        - Account
      responses:
        '200':
          description: The requested compute resource accounts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/accountsGetResponse'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /accounts/{id}:
    get:
      summary: Get account details for a specific compute resource
      description: |
        Get account details for a specific compute resource
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getAccount
      security:
        - token_auth: [ ]
      tags:
        - Account
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The uuid of the compute resource account
      responses:
        '200':
          description: The requested compute resource account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/accountsGetResponse'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    patch:
      summary: Update account details
      description: |
        Partially update the status, name, and/or description of a specific compute resource account. Setting the status
        to PAUSED will disable the use of the account for analytic workflows and all compute nodes on this account. This does not affect storage functions.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: patchAccount
      security:
        - token_auth: [ ]
      tags:
        - Account
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The uuid of the compute resource account
      requestBody:
        description: Account update payload
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum: ["Enabled", "Paused"]
                  description: The new status for the account
                name:
                  type: string
                  description: The new name for the account
                description:
                  type: string
                  description: The new description for the account
      responses:
        '200':
          description: Successfully updated account status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/accountsGetResponse'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    delete:
      summary: Delete a compute resource account
      description: |
        Delete a compute resource account and its workspace enablements. Must be the account owner.
        If the account has active compute nodes, the request will fail with a 409 Conflict unless
        the force query parameter is set to true.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: deleteAccount
      security:
        - token_auth: [ ]
      tags:
        - Account
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The uuid of the compute resource account
        - in: query
          name: force
          required: false
          schema:
            type: string
            enum: ["true", "false"]
          description: If true, delete even if account has active compute nodes
      responses:
        '200':
          description: The deleted account details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/accountsGetResponse'
        '409':
          description: Account has active compute nodes
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes:
    post:
      summary: Create a new compute node
      description: |
        Creates a new compute node for running analytical workflows. This will provision the necessary
        infrastructure in the specified account including ECS tasks, API Gateway, EFS storage, and SQS queues.
        If organization_id is provided, the node will be created within that workspace context.
        If organization_id is omitted, an organization-independent (private) node will be created.

        **Permissions:** Account owner can always create nodes. On public accounts (isPublic=true),
        workspace administrators (permission_bit >= 16) can also create nodes.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: postComputeNodes
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      requestBody:
        description: Compute node configuration
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/computeNodeRequest'
      responses:
        '201':
          description: Successfully created compute node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/computeNodeResponse'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    get:
      summary: List compute nodes
      description: |
        Lists compute nodes accessible to the authenticated user based on access control rules:
        
        **When account_owner=true:**
        - Returns ALL compute nodes on accounts owned by the authenticated user
        - Bypasses individual node permissions and workspace filtering
        - Only works if the user owns at least one compute resource account
        - Useful for billing, management, and auditing purposes
        
        **When no organization_id is provided (normal mode):**
        - Returns only compute nodes owned by the user (organization-independent nodes)
        - These are private nodes created without a workspace context
        
        **When organization_id is provided (normal mode):**
        - Returns nodes accessible to the user within that specific workspace
        - Includes nodes shared with the user directly, through teams, or workspace-wide access
        - Only returns nodes that belong to the specified organization
        
        All responses (except account owner mode) respect the user's actual permissions and access control settings.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getComputeNodes
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: query
          name: organization_id
          schema:
            type: string
          required: false
          description: |
            Workspace ID to filter nodes by. When provided, returns nodes accessible to the user within that workspace.
            When omitted, returns only user-owned organization-independent (private) nodes.
            Ignored when account_owner=true.
        - in: query
          name: account_owner
          schema:
            type: boolean
          required: false
          description: |
            When set to true, returns ALL compute nodes on accounts owned by the authenticated user,
            regardless of individual node permissions or workspace context. Only works if the user 
            is an account owner. Overrides organization_id filtering when true.
            
            This is useful for account owners who need to see all infrastructure deployed 
            on their accounts for billing, management, or auditing purposes.
      responses:
        '200':
          description: List of compute nodes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/computeNodeResponse'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}:
    get:
      summary: Get compute node details
      description: |
        Retrieves detailed information about a specific compute node.

        **Permissions:** Accessible by node owner, users with shared access (direct, team, or workspace scope),
        and workspace administrators on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getComputeNode
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      responses:
        '200':
          description: Compute node details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/computeNodeResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    put:
      summary: Update compute node configuration
      description: |
        Updates the configuration of an existing compute node, such as workflow manager settings
        or authorization type.

        **Permissions:** Node owner, account owner, or workspace administrator on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: putComputeNode
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        description: Compute node update configuration
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/computeNodeUpdateRequest'
      responses:
        '200':
          description: Successfully updated compute node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/computeNodeResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    patch:
      summary: Update compute node details
      description: |
        Partially update the status, name, and/or description of a specific compute node.
        Setting the status to PAUSED will disable the compute node for analytic workflows.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: patchComputeNode
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        description: Compute node update payload
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum: ["Enabled", "Paused"]
                  description: The new status for the compute node
                name:
                  type: string
                  description: The new name for the compute node
                description:
                  type: string
                  description: The new description for the compute node
      responses:
        '200':
          description: Successfully updated compute node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/computeNodeResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    delete:
      summary: Delete a compute node
      description: |
        Deletes a compute node and all its associated infrastructure. This will terminate running tasks,
        delete the API Gateway, EFS storage, and SQS queues.

        **Permissions:** Node owner, account owner, or workspace administrator on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: deleteComputeNode
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node to delete
      responses:
        '204':
          description: Successfully deleted compute node
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/update-config:
    post:
      summary: Update compute node infrastructure configuration
      description: |
        Updates infrastructure-affecting configuration (GPU settings, LLM access) and triggers
        re-provisioning. Only fields included in the request body are changed. The node's status
        must be "Enabled" or "Paused" (not "Pending" or "Destroying").

        **Permissions:** Node owner, account owner, or workspace administrator on public accounts (isPublic=true).
      operationId: postUpdateConfig
      tags:
        - compute-nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/updateConfigRequest'
      responses:
        '202':
          description: Config updated, re-provisioning initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (invalid values or node in wrong state)
        '403':
          description: Forbidden (not node owner, account owner, or workspace administrator on public account)
        '404':
          description: Compute node not found
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/permissions:
    get:
      summary: Get compute node permissions
      description: |
        Retrieves the current permission settings for a compute node.

        **Permissions:** Accessible by node owner, users with shared access (direct, team, or workspace scope),
        and workspace administrators on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getComputeNodePermissions
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      responses:
        '200':
          description: Current permission settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/computeNodePermissionsResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    put:
      summary: Set compute node access scope
      description: |
        Sets the access scope for a compute node.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
        
        Access scope levels:
        - **private**: Only the owner can access the node  
        - **workspace**: All members of the workspace can access the node
        - **shared**: Maintains existing user/team sharing but removes workspace access if any (use POST endpoints to add new shares)
        
        Note: When setting to "shared", existing individual user and team permissions are preserved.
        Use the granular POST/DELETE endpoints on /permissions/users and /permissions/teams to manage specific access grants.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: setComputeNodeAccessScope
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        description: Access scope configuration
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/computeNodeAccessScopeRequest'
      responses:
        '200':
          description: Successfully updated access scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/computeNodePermissionsResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/permissions/users:
    post:
      summary: Grant access to a user
      description: |
        Grants access to a specific user for a compute node.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
        
        **Important**: This operation is only available for compute nodes that belong to a workspace (organization).
        Independent compute nodes cannot be shared with users or teams.
        
        This will automatically set the node's access scope to "shared" if it's currently "private" or "workspace".
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: grantUserAccess
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        description: User to grant access to
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/grantUserAccessRequest'
      responses:
        '201':
          description: Successfully granted access to user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/permissionActionResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/permissions/users/{userId}:
    delete:
      summary: Revoke access from a user
      description: |
        Revokes access from a specific user for a compute node.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
        Cannot revoke access from the node owner.
        
        **Note**: This operation only applies to compute nodes that belong to a workspace (organization).
        Independent compute nodes do not support sharing.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: revokeUserAccess
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
        - in: path
          name: userId
          required: true
          schema:
            type: string
          description: The ID of the user to revoke access from
      responses:
        '200':
          description: Successfully revoked access from user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/permissionActionResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/permissions/teams:
    post:
      summary: Grant access to a team
      description: |
        Grants access to a specific team for a compute node.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
        
        **Important**: This operation is only available for compute nodes that belong to a workspace (organization).
        Independent compute nodes cannot be shared with users or teams.
        
        This will automatically set the node's access scope to "shared" if it's currently "private" or "workspace".
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: grantTeamAccess
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        description: Team to grant access to
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/grantTeamAccessRequest'
      responses:
        '201':
          description: Successfully granted access to team
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/permissionActionResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/permissions/teams/{teamId}:
    delete:
      summary: Revoke access from a team
      description: |
        Revokes access from a specific team for a compute node.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
        
        **Note**: This operation only applies to compute nodes that belong to a workspace (organization).
        Independent compute nodes do not support sharing.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: revokeTeamAccess
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
        - in: path
          name: teamId
          required: true
          schema:
            type: string
          description: The ID of the team to revoke access from
      responses:
        '200':
          description: Successfully revoked access from team
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/permissionActionResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/organization:
    post:
      summary: Attach node to organization
      description: |
        Attaches an organization-independent compute node to an organization. 
        The user must have Collaborator or higher access to the target organization.
        Once attached, the node can be shared with other organization members.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: attachNodeToOrganization
      security:
        - token_workspace_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node to attach
        - in: query
          name: organization_id
          required: true
          schema:
            type: string
          description: The organization ID to attach the node to
      responses:
        '200':
          description: Successfully attached node to organization
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/permissionActionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    delete:
      summary: Detach node from organization
      description: |
        Detaches a compute node from its organization, making it organization-independent 
        and private again. Only the account owner can detach nodes. All shared access
        (users, teams, workspace) will be removed, and the node becomes private to the owner.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: detachNodeFromOrganization
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node to detach
      responses:
        '200':
          description: Successfully detached node from organization
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/permissionActionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/secrets:
    patch:
      summary: Add or update user secrets for a compute node
      description: |
        Merges user-scoped secrets for the specified compute node. New keys are
        added and existing keys are updated; keys not included in the request
        are left unchanged. These secrets are private to the authenticated user
        and will be injected into workflow executions on this node. Requires
        node access (owner, shared, or workspace). Use DELETE to remove secrets.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: patchSecrets
      security:
        - token_auth: [ ]
      tags:
        - Secrets
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/secretsRequest'
      responses:
        '200':
          description: Secrets updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    get:
      summary: Get user secrets for a compute node
      description: |
        Retrieves the user-scoped secrets for the specified compute node.
        Returns only secrets belonging to the authenticated user.
        Requires node access (owner, shared, or workspace).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getSecrets
      security:
        - token_auth: [ ]
      tags:
        - Secrets
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      responses:
        '200':
          description: User secrets for the compute node
          content:
            application/json:
              schema:
                type: object
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    delete:
      summary: Delete user secrets for a compute node
      description: |
        Deletes a specific user-scoped secret by key, or all user secrets
        when deleteAll=true. Only removes secrets belonging to the
        authenticated user. Requires node access (owner, shared, or workspace).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: deleteSecrets
      security:
        - token_auth: [ ]
      tags:
        - Secrets
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
        - in: query
          name: key
          required: false
          schema:
            type: string
          description: The key of the specific secret to delete
        - in: query
          name: deleteAll
          required: false
          schema:
            type: string
            enum: ["true"]
          description: Set to "true" to delete all user secrets
      responses:
        '200':
          description: Secret(s) deleted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/shared-secrets:
    patch:
      summary: Add or update shared secrets for a compute node
      description: |
        Merges shared secrets for the specified compute node. New keys are
        added and existing keys are updated; keys not included in the request
        are left unchanged. These secrets are visible to all users with access
        to the node. Use DELETE to remove secrets.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: patchSharedSecrets
      security:
        - token_auth: [ ]
      tags:
        - Secrets
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/secretsRequest'
      responses:
        '200':
          description: Shared secrets updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    get:
      summary: Get shared secrets for a compute node
      description: |
        Retrieves the shared secrets for the specified compute node.
        Any user with access to the node can read shared secrets.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getSharedSecrets
      security:
        - token_auth: [ ]
      tags:
        - Secrets
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      responses:
        '200':
          description: Shared secrets for the compute node
          content:
            application/json:
              schema:
                type: object
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    delete:
      summary: Delete shared secrets for a compute node
      description: |
        Deletes a specific shared secret by key, or all shared secrets
        when deleteAll=true.

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: deleteSharedSecrets
      security:
        - token_auth: [ ]
      tags:
        - Secrets
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
        - in: query
          name: key
          required: false
          schema:
            type: string
          description: The key of the specific shared secret to delete
        - in: query
          name: deleteAll
          required: false
          schema:
            type: string
            enum: ["true"]
          description: Set to "true" to delete all shared secrets
      responses:
        '200':
          description: Shared secret(s) deleted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/config:
    get:
      summary: Get compute node runtime configuration
      description: |
        Returns the runtime configuration for the specified compute node,
        such as health check thresholds. The account-service proxies this
        request to the compute node's gateway.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getComputeNodeConfig
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      responses:
        '200':
          description: Current runtime configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/nodeConfigResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    patch:
      summary: Update compute node runtime configuration
      description: |
        Partially updates the runtime configuration for the specified compute
        node. Only fields present in the request body are updated; omitted
        fields retain their current values. The account-service proxies this
        request to the compute node's gateway.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: patchComputeNodeConfig
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/nodeConfigRequest'
      responses:
        '200':
          description: Updated runtime configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/nodeConfigResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /compute-nodes/{id}/allowed-processors:
    put:
      summary: Set allowed processors for a compute node
      description: |
        Replaces the processor whitelist for the specified compute node.
        When the list is non-empty, only processors whose source URLs match
        may be executed on this node. An empty list allows all processors (open mode).

        **Permissions:** Node owner or workspace administrator on public accounts (isPublic=true).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: putAllowedProcessors
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/allowedProcessorsRequest'
      responses:
        '200':
          description: Allowed processors updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
    get:
      summary: Get allowed processors for a compute node
      description: |
        Returns the processor whitelist for the specified compute node.
        An empty list means all processors are allowed (open mode).
        Requires node access (owner, shared, or workspace).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getAllowedProcessors
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: The UUID of the compute node
      responses:
        '200':
          description: Allowed processors list
          content:
            application/json:
              schema:
                type: object
                properties:
                  processors:
                    type: array
                    items:
                      type: string
                    description: |
                      List of allowed processor source URL patterns. Each entry is a
                      source URL (e.g. "git://github.com/org/repo:v1"). Exact match,
                      or omit the tag to allow any version. Empty list means all
                      processors are allowed.
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /app-store/access:
    post:
      summary: Grant App Store ECR access
      description: |
        Grants all registered AWS accounts cross-account pull access to the
        private App Store ECR repository. All account principals are consolidated
        into a single repository policy statement.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: postAppStoreAccess
      x-internal: true
      security:
        - token_auth: [ ]
      tags:
        - Compute Nodes
      requestBody:
        description: Account requesting App Store access
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - accountId
                - accountType
              properties:
                accountId:
                  type: string
                  description: The AWS account ID requesting access
                accountType:
                  type: string
                  description: The type of the account
      responses:
        '200':
          description: Successfully updated ECR repository policy
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /role-policy:
    get:
      summary: Get IAM role policy
      description: |
        Returns the scoped IAM permission policy document that agents use when
        creating cross-account roles. This is the single source of truth for
        the set of AWS actions the provisioner requires.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getRolePolicy
      security:
        - token_auth: [ ]
      tags:
        - Account
      responses:
        '200':
          description: The IAM permission policy document
          content:
            application/json:
              schema:
                type: object
                description: An IAM policy document with Version and Statement fields
        '4XX':
          $ref: '#/components/responses/Unauthorized'
        '5XX':
          $ref: '#/components/responses/Error'
  /storage-nodes:
    post:
      summary: Create a storage node
      description: Creates a new storage node associated with an account.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: createStorageNode
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - accountUuid
                - name
                - storageLocation
                - providerType
              properties:
                accountUuid:
                  type: string
                organizationId:
                  type: string
                  description: |
                    Optional workspace for this storage node. When provided, the account must be
                    enabled for the workspace, the caller must be the account owner or a workspace
                    admin on a public+storage-enabled account, and the node is auto-attached to
                    that workspace. When omitted, only the account owner may create the node and
                    no workspace attachment is created (workspaces can be attached afterwards via
                    POST /storage-nodes/{id}/workspace).
                  example: "N:organization:050fae39-4412-43ef-a514-703ed8e299d5"
                name:
                  type: string
                description:
                  type: string
                storageLocation:
                  type: string
                  description: S3 bucket name, Azure container URL, or local path
                region:
                  type: string
                providerType:
                  type: string
                  enum: ["s3", "azure-blob", "local"]
      responses:
        '201':
          description: Storage node created
        '400':
          description: Bad request
        '403':
          description: Forbidden
    get:
      summary: List storage nodes
      description: Lists storage nodes filtered by organization_id or account_owner=true.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: listStorageNodes
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: organization_id
          in: query
          schema:
            type: string
        - name: account_owner
          in: query
          schema:
            type: string
      responses:
        '200':
          description: List of storage nodes
        '400':
          description: Bad request
  /storage-nodes/{id}:
    get:
      summary: Get a storage node
      description: Retrieves a single storage node by ID.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getStorageNode
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Storage node details
        '403':
          description: Forbidden
        '404':
          description: Not found
    patch:
      summary: Update a storage node
      description: Updates storage node fields (name, description, storageLocation, region, status).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: updateStorageNode
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                status:
                  type: string
                  enum: ["Enabled", "Disabled"]
      responses:
        '200':
          description: Storage node updated
        '403':
          description: Forbidden
        '404':
          description: Not found
    delete:
      summary: Delete a storage node
      description: Deletes a storage node. Account owner only.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: deleteStorageNode
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Storage node deleted
        '403':
          description: Forbidden
        '404':
          description: Not found
  /storage-nodes/{id}/update-config:
    post:
      summary: Update storage node infrastructure config
      description: |
        Re-applies the provisioner terraform on an existing storage node to update
        bucket configuration (CORS, lifecycle, encryption settings, etc.) without
        touching bucket contents. The bucket has prevent_destroy=true in terraform.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: updateStorageNodeConfig
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Config update initiated
        '403':
          description: Forbidden
        '404':
          description: Not found
  /storage-nodes/{id}/workspace:
    post:
      summary: Attach storage node to workspace
      description: Attaches a storage node to a workspace. Account owner only.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: attachStorageNodeToWorkspace
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - workspaceId
              properties:
                workspaceId:
                  type: string
                isDefault:
                  type: boolean
      responses:
        '201':
          description: Storage node attached to workspace
        '403':
          description: Forbidden
        '409':
          description: Already attached
    delete:
      summary: Detach storage node from workspace
      description: Detaches a storage node from a workspace. Account owner only.
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: detachStorageNodeFromWorkspace
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - workspaceId
              properties:
                workspaceId:
                  type: string
      responses:
        '200':
          description: Storage node detached from workspace
        '403':
          description: Forbidden
        '409':
          description: Cannot detach - data exists
  /storage-nodes/{id}/impact:
    get:
      summary: Get detach impact
      description: Returns the impact of detaching a storage node (affected datasets, files, size).
      x-amazon-apigateway-integration:
        $ref: '#/components/x-amazon-apigateway-integrations/account-service'
      operationId: getStorageNodeDetachImpact
      security:
        - token_auth: [ ]
      tags:
        - Storage Nodes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: organization_id
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Impact analysis
          content:
            application/json:
              schema:
                type: object
                properties:
                  datasets:
                    type: integer
                  files:
                    type: integer
                  totalSizeBytes:
                    type: integer
        '403':
          description: Forbidden
        '404':
          description: Not found